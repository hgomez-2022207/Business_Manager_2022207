import { Router } from "express";
import { check } from "express-validator";
import {} from "./empresa.controller";
import { Router } from "express";

const router =  Router();

export default router;